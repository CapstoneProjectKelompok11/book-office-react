import React, { useEffect, useState } from "react";

const Listing = ({item, hapus}) => {
    return (
        <tr>
            {/* <td><p>{item.title}</p></td>
            <td>
                <button type="button" class="btn btn-danger" onClick={() => {hapus(item.id)}}>Delete</button>
            </td> */}
        </tr>
    )
}